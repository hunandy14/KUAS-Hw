/**********************************************************
Name : OpenRaw 2.21
Date : 2016/08/04
By   : CharlotteHonG
Final: 2016/08/25
**********************************************************/
#ifndef OPEN_RAW
#define OPEN_RAW


#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <iomanip>
#include <sstream>
using namespace std;
typedef unsigned char imch;
typedef unsigned int imint;

namespace imr{
    class ImrSize{
    public:
        ImrSize(imint y, imint x);
        imint width;
        imint high;
    };
    class ImrCoor{
    public:
        ImrCoor(imint y, imint x);
        imint y;
        imint x;
    };
    // class ImrMask{
    // public:
    //     ImrMask();
    //     ~ImrMask();
    // private:
    //     imch* mask;
    //     imint high;
    //     imint width;
    // };

    class imgraw {
    public:
        imgraw(ImrSize size);
        void read(string filename);
        void write(string filename);
        imch point_read(imint y, imint x);
        void point_write(imint y, imint x, imch value);
        void resize_canvas(ImrSize size);
        imint w();
        imint h();
        void pri_htg(string title);
        void setMaskSize(ImrSize masksize);
        imch mask(ImrCoor ori, ImrCoor tar,
             int sy, int sx);

    private:
        ifstream::pos_type filesize;
        string filename;
        vector<imch> img_data;
        imint width;
        imint high;
        imch max;
        imch min;
        int htg_data[255];
        void histogram();
        void extremum();
        ImrSize masksize;
    };

#include "OpenRAW.cpp"
};



#endif